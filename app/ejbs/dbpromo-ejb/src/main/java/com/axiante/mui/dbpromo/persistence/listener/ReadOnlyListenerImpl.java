package com.axiante.mui.dbpromo.persistence.listener;

import javax.ejb.Stateless;

@Stateless
public class ReadOnlyListenerImpl implements ReadOnlyListener {

	@Override
	public void invoke(Object o) {
	}
}
