apiVersion: cloud.google.com/v1beta1
kind: BackendConfig
metadata:
  name: mui-becfg
  labels:
    {{- include "mui_app.labels" . | nindent 4 }}
spec:
  sessionAffinity:
    affinityType: "CLIENT_IP"
